# Server boundary

This directory is reserved for future backend or operator-console work that replaces the legacy Swing socket server with authenticated, audited, and policy-gated services.

Planned safe work:

- authenticated API gateway;
- device inventory;
- telemetry ingestion;
- role-based operator console;
- immutable audit logging.
