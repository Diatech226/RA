# Protocol boundary

This directory documents the old shared protocol boundary. Future development should use versioned schemas, authenticated transport, replay protection, and strict validation.

Do not implement unauthenticated command-and-control packet handling here.
