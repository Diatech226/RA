# API and dependency boundary

This directory is reserved for third-party integration notes and future API contracts. Prefer dependency managers and reproducible builds instead of vendoring opaque libraries.
